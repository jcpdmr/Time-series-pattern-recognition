{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Debug",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "preLaunchTask": "Build CUDA Application"
        },
    ]
}